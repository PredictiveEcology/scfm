After knitting and rendering `scfmDataPrep.Rmd`, overwrite this file with a symlink to `scfmDataPrep.md`.

E.g., on Linux/macOS from terminal:

```bash
cd C:/Ian/Git/scfm/modules/scfmDataPrep
rm README.md && ln -s scfmDataPrep.md README.md
```
